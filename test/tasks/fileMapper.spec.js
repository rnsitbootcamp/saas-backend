
// const { expect } = require('chai');
const FileMapper = require('../../dist/processor/FileMapper').default;

describe('Test-tasks', function () {
    before(function (done) {
        done();
    });

    after(function (done) {
        done();
    });

    it('ok', function (done) {
        // this.timeout(5000);

    });
});